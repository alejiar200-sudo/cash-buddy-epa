# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Dependencies
node_modules

# Build outputs
dist
build
out
.next
.output

# Electron build artifacts
apps/desktop/release
apps/desktop/dist

# Env
*.local
.env
.env.*
!.env.example

# Prisma
apps/api/prisma/*.db

# Secreto de cifrado de API Keys (se genera por equipo, NO se versiona)
.encryption-key
apps/api/.encryption-key

# Artefactos del lanzador de escritorio
.cashbuddy-app-profile/
cashbuddy-launcher.log

# Respaldos de base de datos (contienen API Key cifrada, hashes y datos sensibles)
backups/
*.sql
# Excepción: las migraciones de Prisma SÍ deben versionarse (si no, otra sucursal
# que clone el repo no tendría los cambios de esquema y el sistema fallaría).
!apps/api/prisma/migrations/**/*.sql

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.sln
*.sw?
